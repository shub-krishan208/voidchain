# Server-side upstream target for Next.js BFF route handlers.
# If omitted, the app falls back to http://localhost:4040.
VOIDCHAIN_API_URL=http://localhost:4040

# Optional public hint, useful if you need the URL in client-only code later.
NEXT_PUBLIC_VOIDCHAIN_API_URL=http://localhost:4040
